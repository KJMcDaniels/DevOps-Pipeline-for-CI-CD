commands:
  01_install_node_package_manager:
    command: |
      npm install -g pnpm
    test: "! pnpm --version"  # This command runs if pnpm is not installed

option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
    NPM_CONFIG_LOGLEVEL: error